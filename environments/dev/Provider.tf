# Configure the Microsoft Azure Provider
# use environment variables instead of harcoding these
provider "azurerm" {
  subscription_id = "X"
  client_id       = "X"
  client_secret   = "X"
  tenant_id       = "X"
}
